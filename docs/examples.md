# Расширенные примеры использования

## 1. Автоматизация поиска и анализа

### Поиск информации в Google Scholar

```
Задача: Найти научные статьи по теме "machine learning"

1. Запусти браузер
2. Открой https://scholar.google.com
3. Найди поле поиска (selector: input[name="q"])
4. Введи "machine learning applications 2024"
5. Нажми кнопку поиска
6. Подожди загрузки результатов
7. Получи заголовки первых 5 статей с помощью JavaScript:
   script: "return Array.from(document.querySelectorAll('.gs_rt a')).slice(0, 5).map(a => a.textContent)"
8. Сделай скриншот для отчета
```

## 2. Веб-скрапинг данных

### Извлечение цен с интернет-магазина

```
Задача: Собрать цены на товары

1. Открой страницу магазина
2. Найди все элементы с ценами: document.querySelectorAll('.price')
3. Выполни JavaScript для извлечения:
   script: "return Array.from(document.querySelectorAll('.price')).map(el => ({
     text: el.textContent,
     value: parseFloat(el.textContent.replace(/[^0-9.]/g, ''))
   }))"
4. Сохрани результаты
```

## 3. Тестирование веб-приложений

### Проверка формы регистрации

```
Задача: Протестировать валидацию формы

1. Открой страницу регистрации
2. Попробуй отправить пустую форму (кликнуть submit без заполнения)
3. Проверь наличие сообщений об ошибках:
   find_element: ".error-message"
4. Заполни поле email некорректным адресом: "invalid-email"
5. Проверь валидацию email
6. Заполни все поля корректно
7. Отправь форму
8. Проверь редирект на страницу успеха
```

## 4. Автоматизация рутинных задач

### Автоматический вход на сайт

```
Задача: Войти в учетную запись

ВНИМАНИЕ: Будьте осторожны с чувствительными данными!

1. Открой страницу входа
2. Найди поле email/username: input[name="username"]
3. Введи имя пользователя
4. Найди поле пароля: input[type="password"]
5. Введи пароль
6. Найди checkbox "Запомнить меня" (если нужно)
7. Кликни на кнопку входа
8. Дождись загрузки личного кабинета
9. Проверь успешность входа по наличию элемента профиля
```

### Мониторинг изменений на странице

```
Задача: Отследить изменения на странице

1. Открой целевую страницу
2. Получи текущий контент:
   script: "return document.querySelector('.target-content').textContent"
3. Сохрани контент в переменную
4. Подожди 60 секунд (настрой интервал)
5. Обнови страницу: browser_refresh
6. Получи новый контент
7. Сравни с сохраненным
8. Если изменился - сделай скриншот и уведоми
```

## 5. Работа с динамическим контентом

### Прокрутка бесконечной ленты

```
Задача: Загрузить все элементы бесконечной ленты

1. Открой страницу с лентой (например, социальная сеть)
2. Выполни JavaScript для прокрутки:
   script: "window.scrollTo(0, document.body.scrollHeight)"
3. Подожди подгрузки (execute_script для проверки)
4. Повтори прокрутку 5-10 раз
5. Собери все загруженные элементы:
   script: "return document.querySelectorAll('.post').length"
```

### Взаимодействие с модальными окнами

```
Задача: Работа с popup окнами

1. Открой страницу
2. Кликни на кнопку, открывающую модальное окно
3. Дождись появления модального окна:
   find_element: ".modal.show"
4. Заполни поля внутри модального окна
5. Кликни на кнопку подтверждения в модальном окне
6. Проверь закрытие модального окна
```

## 6. Извлечение структурированных данных

### Парсинг таблицы данных

```
Задача: Извлечь данные из HTML таблицы

1. Открой страницу с таблицей
2. Выполни JavaScript для извлечения:
   script: `
   const table = document.querySelector('table');
   const headers = Array.from(table.querySelectorAll('th')).map(th => th.textContent);
   const rows = Array.from(table.querySelectorAll('tbody tr')).map(tr => 
     Array.from(tr.querySelectorAll('td')).map(td => td.textContent)
   );
   return { headers, rows };
   `
3. Обработай полученные данные
```

### Сбор ссылок со страницы

```
Задача: Собрать все ссылки определенного типа

1. Открой страницу
2. Выполни JavaScript:
   script: `
   return Array.from(document.querySelectorAll('a[href]'))
     .map(a => ({
       text: a.textContent.trim(),
       href: a.href,
       internal: a.hostname === window.location.hostname
     }))
     .filter(link => link.internal);
   `
```

## 7. Скриншоты и визуальное тестирование

### Создание скриншотов всех страниц сайта

```
Задача: Создать скриншоты основных страниц

1. Определи список URL страниц
2. Для каждого URL:
   a. Открой страницу: navigate
   b. Дождись полной загрузки:
      execute_script: "return document.readyState === 'complete'"
   c. Сделай скриншот с именем файла по названию страницы
   d. Переходи к следующей
```

## 8. Работа с cookies и local storage

### Сохранение сессии

```
Задача: Получить cookies для сохранения сессии

1. Открой сайт и войди
2. Получи cookies через JavaScript:
   script: "return document.cookie"
3. Получи localStorage:
   script: "return JSON.stringify(localStorage)"
4. Сохрани данные для последующего восстановления сессии
```

### Установка cookies

```
Задача: Восстановить сессию из cookies

1. Открой сайт
2. Установи cookies через JavaScript:
   script: "document.cookie = 'session_id=abc123; path=/'"
3. Обнови страницу
4. Проверь что сессия восстановлена
```

## 9. Продвинутые техники

### Ожидание AJAX запросов

```
Задача: Дождаться завершения AJAX

1. Открой страницу
2. Кликни на кнопку, запускающую AJAX
3. Используй JavaScript для ожидания:
   script: `
   return new Promise((resolve) => {
     const checkAjax = setInterval(() => {
       if (jQuery.active === 0) {
         clearInterval(checkAjax);
         resolve(true);
       }
     }, 100);
   });
   `
```

### Работа с Shadow DOM

```
Задача: Доступ к элементам в Shadow DOM

1. Открой страницу с web components
2. Получи элемент из Shadow DOM:
   script: `
   const host = document.querySelector('my-component');
   const shadowRoot = host.shadowRoot;
   return shadowRoot.querySelector('.inner-element').textContent;
   `
```

## 10. Отладка и мониторинг

### Получение console.log из браузера

```
Задача: Отслеживать console.log

1. Открой страницу
2. Включи перехват логов:
   script: `
   window.logs = [];
   const originalLog = console.log;
   console.log = function(...args) {
     window.logs.push(args.join(' '));
     originalLog.apply(console, args);
   };
   `
3. Выполни действия на странице
4. Получи логи:
   script: "return window.logs"
```

### Проверка производительности

```
Задача: Измерить время загрузки страницы

1. Открой страницу
2. Получи метрики производительности:
   script: `
   const timing = performance.timing;
   return {
     loadTime: timing.loadEventEnd - timing.navigationStart,
     domReady: timing.domContentLoadedEventEnd - timing.navigationStart,
     responseTime: timing.responseEnd - timing.requestStart
   };
   `
```

## Советы по использованию

### Обработка ошибок

Всегда проверяйте результат операций:
```python
result = browser.click("button.submit")
if not result["success"]:
    print(f"Ошибка: {result.get('error', 'Unknown error')}")
    # Попробуйте альтернативный способ или завершите
```

### Использование правильных селекторов

Порядок предпочтения:
1. ID: `#unique-id` - самый надежный
2. Data-атрибуты: `[data-testid="submit-btn"]`
3. CSS классы: `.btn.btn-primary`
4. XPath: `//button[contains(text(), 'Submit')]`

### Таймауты и ожидания

Для медленно загружающихся страниц можно увеличить таймаут в коде `browser_manager.py`.

Удачи в автоматизации! 🚀
